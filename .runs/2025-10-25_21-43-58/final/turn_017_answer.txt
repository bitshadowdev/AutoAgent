Debido a limitaciones de seguridad, no tengo acceso directo a tu sistema de archivos. Para crear un frontend HTML que se integre con la lógica de "runs" y "eventos", por favor copia y pega aquí:

1. Los archivos clave de la carpeta **coreee** (clases, funciones, módulos o exportaciones que manejan los runs y los eventos).
2. Un ejemplo o la estructura JSON de los archivos que se encuentran en la carpeta **.runs** (para saber cómo están estructurados los datos de runs y eventos).
3. Si existe alguna API interna o funciones exportadas que el JavaScript del frontend deba invocar, incluye su firma o una breve descripción.

Con esa información podré diseñar una plantilla HTML autónoma con los contenedores necesarios, el script JavaScript para cargar los datos y estilos CSS básicos.